using UnityEngine;
using System.Collections;

public class TriggerNumbering : MonoBehaviour {

	// Use this for initialization
	void Start () {
		MeshRenderer rnd = this.GetComponent<MeshRenderer>();
		rnd.enabled = false;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
